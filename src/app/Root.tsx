import React from 'react';

import CssBaseLine from '@material-ui/core/CssBaseline';

const Root: React.FC = () => {
  return (
    <React.Fragment>
      <CssBaseLine/>
    </React.Fragment>
  );
};

export default Root;
